{
  "name": "agentchat-cloud",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Mobile-first Cloudflare Browser Run frontend for AgentChat",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run --outdir .wrangler-dry-run"
  },
  "dependencies": {
    "@cloudflare/playwright": "^1.3.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
